# 🏗️ Report System Architecture

## System Flow Diagram

```
┌─────────────────────────────────────────────────────────────────┐
│                         FRONTEND (Flutter)                       │
├─────────────────────────────────────────────────────────────────┤
│                                                                   │
│  ┌──────────────┐         ┌──────────────────────────────┐     │
│  │ Product Page │────────▶│  ReportItemDialog           │     │
│  │              │         │  - itemId                    │     │
│  │ [Flag Icon]  │         │  - itemTitle                 │     │
│  └──────────────┘         │  - description (user input)  │     │
│                           └──────────┬───────────────────┘     │
│                                      │                          │
│                                      ▼                          │
│                           ┌──────────────────────┐              │
│                           │   ApiService         │              │
│                           │   createReport()     │              │
│                           └──────────┬───────────┘              │
│                                      │                          │
└──────────────────────────────────────┼──────────────────────────┘
                                       │
                                       │ HTTP POST
                                       │ /reports
                                       │
                                       ▼
┌─────────────────────────────────────────────────────────────────┐
│                       BACKEND (.NET)                             │
├─────────────────────────────────────────────────────────────────┤
│                                                                   │
│  ┌────────────────────────────────────────────────────┐         │
│  │  CreateReportHandler                               │         │
│  │  1. Validate item exists                           │         │
│  │  2. Validate user exists                           │         │
│  │  3. Find moderator with least PENDING reports      │         │
│  │  4. Create report with ModeratorId                 │         │
│  │  5. Save to database                               │         │
│  └────────────────────┬───────────────────────────────┘         │
│                       │                                          │
│                       ▼                                          │
│  ┌──────────────────────────────────────────┐                   │
│  │         DATABASE (Entity Framework)       │                   │
│  │  ┌────────────────────────────────────┐  │                   │
│  │  │ Reports Table                      │  │                   │
│  │  │ - Id (PK)                          │  │                   │
│  │  │ - ItemId (FK → Items)              │  │                   │
│  │  │ - OwnerId (FK → Users)             │  │                   │
│  │  │ - UserId (FK → Users)              │  │                   │
│  │  │ - Description                      │  │                   │
│  │  │ - Status (enum)                    │  │                   │
│  │  │ - ModeratorId (FK → Users)         │  │                   │
│  │  │ - CreatedDate                      │  │                   │
│  │  └────────────────────────────────────┘  │                   │
│  └──────────────────────────────────────────┘                   │
│                                                                   │
└─────────────────────────────────────────────────────────────────┘
                                       │
                                       │
                                       ▼
┌─────────────────────────────────────────────────────────────────┐
│                    MODERATOR INTERFACE                           │
├─────────────────────────────────────────────────────────────────┤
│                                                                   │
│  ┌──────────────────────────────────────────────────┐           │
│  │  MainPage Drawer                                  │           │
│  │  - Check user role from JWT token                │           │
│  │  - Show "Moderator Reports" if Admin/Moderator   │           │
│  └──────────────┬───────────────────────────────────┘           │
│                 │                                                │
│                 ▼                                                │
│  ┌──────────────────────────────────────────────────┐           │
│  │  ModeratorReportsPage                            │           │
│  │  ┌────────────────────────────────────────────┐ │           │
│  │  │ GET /reports/moderator/{moderatorId}       │ │           │
│  │  └────────────┬───────────────────────────────┘ │           │
│  │               │                                  │           │
│  │               ▼                                  │           │
│  │  ┌────────────────────────────────────────────┐ │           │
│  │  │ Filter Tabs: ALL | PENDING | ACCEPTED |    │ │           │
│  │  │              DECLINED                       │ │           │
│  │  └────────────────────────────────────────────┘ │           │
│  │                                                  │           │
│  │  ┌────────────────────────────────────────────┐ │           │
│  │  │ Report Cards (for each report):            │ │           │
│  │  │  - Status badge                            │ │           │
│  │  │  - Description                             │ │           │
│  │  │  - Item ID, Reporter ID                    │ │           │
│  │  │  - [Accept] [Decline] buttons (if pending) │ │           │
│  │  └────────────────────────────────────────────┘ │           │
│  │                                                  │           │
│  │               │                                  │           │
│  │               ▼ [Accept/Decline clicked]        │           │
│  │                                                  │           │
│  │  ┌────────────────────────────────────────────┐ │           │
│  │  │ PATCH /reports/{reportId}                  │ │           │
│  │  │ Body: { status, moderatorId }              │ │           │
│  │  └────────────────────────────────────────────┘ │           │
│  └──────────────────────────────────────────────────┘           │
│                                                                   │
└─────────────────────────────────────────────────────────────────┘
```

---

## Component Interaction Map

```
┌─────────────────────────────────────────────────────────────┐
│                      User Actions                            │
└─────────────────────────────────────────────────────────────┘
                              │
              ┌───────────────┴───────────────┐
              │                               │
              ▼                               ▼
    ┌──────────────────┐          ┌──────────────────────┐
    │  Report an Item  │          │  Review Reports      │
    │  (Regular User)  │          │  (Moderator/Admin)   │
    └─────────┬────────┘          └──────────┬───────────┘
              │                               │
              │                               │
              ▼                               ▼
    ┌──────────────────┐          ┌──────────────────────┐
    │ ReportItemDialog │          │ ModeratorReportsPage │
    └─────────┬────────┘          └──────────┬───────────┘
              │                               │
              │                               │
              └───────────────┬───────────────┘
                              │
                              ▼
                    ┌──────────────────┐
                    │   ApiService     │
                    └─────────┬────────┘
                              │
              ┌───────────────┼───────────────┐
              │               │               │
              ▼               ▼               ▼
    ┌─────────────┐  ┌──────────────┐  ┌──────────────┐
    │ createReport│  │getReportsByMo│  │updateReportSt│
    │             │  │derator        │  │atus          │
    └──────┬──────┘  └──────┬───────┘  └──────┬───────┘
           │                │                  │
           └────────────────┼──────────────────┘
                            │
                            ▼
              ┌──────────────────────────┐
              │    Backend Endpoints     │
              │                          │
              │  POST   /reports         │
              │  GET    /reports/...     │
              │  PATCH  /reports/{id}    │
              └──────────┬───────────────┘
                         │
                         ▼
              ┌──────────────────────────┐
              │   MediatR Handlers       │
              │                          │
              │  CreateReportHandler     │
              │  GetReportsByModerator.. │
              │  UpdateReportStatus..    │
              └──────────┬───────────────┘
                         │
                         ▼
              ┌──────────────────────────┐
              │  ApplicationContext      │
              │  (Entity Framework)      │
              └──────────┬───────────────┘
                         │
                         ▼
              ┌──────────────────────────┐
              │       Database           │
              │  (Reports, Users, Items) │
              └──────────────────────────┘
```

---

## Data Flow: Creating a Report

```
1. User clicks Flag icon on Product Page
                  │
                  ▼
2. ReportItemDialog opens
   - Shows item title
   - User types description
                  │
                  ▼
3. User clicks "Submit Report"
   - Validation runs (10-1000 chars)
                  │
                  ▼
4. ApiService.createReport() called
   - itemId: from product page
   - userId: extracted from JWT token
   - description: user input
                  │
                  ▼
5. HTTP POST to /reports
   - Authorization: Bearer {token}
   - Content-Type: application/json
   - Body: { itemId, userId, description }
                  │
                  ▼
6. Backend: CreateReportHandler
   - Validate item exists
   - Validate user exists
   - Find moderator with least pending reports
   - Set: Status = PENDING
   - Set: CreatedDate = NOW
   - Set: ModeratorId = {selected moderator}
   - Set: OwnerId = {item owner}
                  │
                  ▼
7. Save to Database
   - INSERT INTO Reports
                  │
                  ▼
8. Return Report DTO (201 Created)
                  │
                  ▼
9. Frontend shows success message
   - Close dialog
   - Show SnackBar
```

---

## Data Flow: Moderator Reviews Report

```
1. Moderator opens Drawer → "Moderator Reports"
                  │
                  ▼
2. ModeratorReportsPage loads
   - Extract moderatorId from JWT
                  │
                  ▼
3. ApiService.getReportsByModerator(moderatorId)
                  │
                  ▼
4. HTTP GET /reports/moderator/{moderatorId}
   - Authorization: Bearer {token}
                  │
                  ▼
5. Backend: GetReportsByModeratorHandler
   - Query: WHERE ModeratorId = {moderatorId}
   - OrderBy: CreatedDate DESC
                  │
                  ▼
6. Return List<ReportDto>
                  │
                  ▼
7. Frontend displays report cards
   - Filter tabs with counts
   - Color-coded status badges
   - Action buttons for PENDING
                  │
                  ▼
8. Moderator clicks "Accept" or "Decline"
                  │
                  ▼
9. Confirmation dialog appears
                  │
                  ▼
10. ApiService.updateReportStatus()
    - reportId
    - status: "ACCEPTED" | "DECLINED"
    - moderatorId
                  │
                  ▼
11. HTTP PATCH /reports/{reportId}
    - Body: { status, moderatorId }
                  │
                  ▼
12. Backend: UpdateReportStatusHandler
    - Find report by ID
    - Validate moderator exists
    - Update: Status = {new status}
    - Update: ModeratorId = {moderator}
    - Save to database
                  │
                  ▼
13. Return updated Report DTO (200 OK)
                  │
                  ▼
14. Frontend refreshes report list
    - Show success message
    - Report moves to appropriate filter tab
```

---

## Security Layer

```
┌─────────────────────────────────────────────────────────────┐
│                    JWT Token Flow                            │
└─────────────────────────────────────────────────────────────┘

Login → Backend issues JWT token
         │
         │ Token contains:
         │  - sub: userId
         │  - email: user@example.com
         │  - role: ["Admin", "Moderator", "User"]
         │  - emailVerified: true/false
         │  - exp: expiration timestamp
         │
         ▼
Token stored in SecureStorageService
         │
         ▼
Every API call includes:
  Authorization: Bearer {token}
         │
         ▼
Backend validates token:
  - Signature valid?
  - Not expired?
  - Has required role?
         │
         ├─ ✅ Valid → Process request
         │
         └─ ❌ Invalid → 401 Unauthorized

Frontend checks role:
  ApiService.isAdminOrModerator(token)
    │
    ├─ true → Show "Moderator Reports" menu
    │
    └─ false → Hide menu option
```

---

## Database Schema

```sql
┌──────────────────────────────────────────────────────────┐
│                      Reports Table                        │
├──────────────────────────────────────────────────────────┤
│ Id             UNIQUEIDENTIFIER  PRIMARY KEY              │
│ ItemId         UNIQUEIDENTIFIER  FOREIGN KEY → Items.Id  │
│ OwnerId        UNIQUEIDENTIFIER  FOREIGN KEY → Users.Id  │
│ UserId         UNIQUEIDENTIFIER  FOREIGN KEY → Users.Id  │
│ Description    NVARCHAR(1000)    NOT NULL                │
│ CreatedDate    DATETIME          NOT NULL, DEFAULT NOW   │
│ Status         INT               NOT NULL, DEFAULT 0     │
│                                  (0=PENDING, 1=ACCEPTED, │
│                                   2=DECLINED)            │
│ ModeratorId    UNIQUEIDENTIFIER  NULLABLE, FK → Users.Id │
└──────────────────────────────────────────────────────────┘

Indexes:
- PK_Reports: Id
- IX_Reports_ItemId: ItemId
- IX_Reports_ModeratorId: ModeratorId
- IX_Reports_Status: Status
- IX_Reports_CreatedDate: CreatedDate DESC
```

---

## State Management

```
ModeratorReportsPage State:
┌────────────────────────────────────┐
│ _reports: List<Map>                │ ← All reports for moderator
│ _isLoading: bool                   │ ← Loading indicator
│ _errorMessage: String?             │ ← Error state
│ _filterStatus: String              │ ← Current filter (ALL/PENDING/etc)
└────────────────────────────────────┘

ReportItemDialog State:
┌────────────────────────────────────┐
│ _descriptionController: TextEditingController │
│ _isLoading: bool                   │ ← Submission in progress
│ _errorMessage: String?             │ ← Error message
└────────────────────────────────────┘
```

---

## API Endpoint Summary

| Method | Endpoint | Auth | Description |
|--------|----------|------|-------------|
| POST | `/reports` | User | Create report |
| GET | `/reports` | Admin | Get all reports |
| GET | `/reports/item/{itemId}` | Admin | Reports for item |
| GET | `/reports/moderator/{modId}` | Admin/Mod | Moderator's reports |
| GET | `/reports/item/{itemId}/accepted-last-week?numberOfDays={n}` | Public | Count accepted |
| PATCH | `/reports/{reportId}` | Admin/Mod | Update status |

---

## File Structure

```
Frontend/unishare_web/lib/
├── models/
│   └── report.dart                 ← Data models
├── services/
│   └── api_service.dart            ← API methods (modified)
├── screens/
│   ├── report_item_dialog.dart     ← Report dialog (new)
│   ├── moderator_reports_page.dart ← Moderator UI (new)
│   ├── product_page.dart           ← Added report button (modified)
│   └── main_page.dart              ← Added menu item (modified)

Backend/Features/Reports/
├── CreateReport/
│   ├── CreateReportHandler.cs
│   └── CreateReportRequest.cs
├── GetAllReports/
├── GetReportsByItem/
├── GetReportsByModerator/
├── GetAcceptedReportsCount/
├── UpdateReportStatus/
├── DTO/
│   ├── CreateReportDto.cs
│   ├── ReportDto.cs
│   └── UpdateReportStatusDto.cs
└── Enums/
    └── ReportStatus.cs

Backend/Data/
└── Report.cs                       ← Entity model
```

---

This architecture ensures:
✅ Clear separation of concerns
✅ Type-safe data flow
✅ Role-based access control
✅ Scalable moderator assignment
✅ Comprehensive error handling
✅ Efficient database queries

