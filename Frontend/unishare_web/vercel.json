{
  "buildCommand": "flutter build web --dart-define=API_BASE_URL=$BACKEND_URL",
  "outputDirectory": "build/web",
  "rewrites": [
    {
      "source": "/(.*)",
      "destination": "/index.html"
    }
  ]
}